{% extends 'base.html.twig' %}

{% block body %}
  <div class="wrapper">
    <p class="question">Was ist die Ticketnummer von Marc’s erstem Ticket bei Shopware?</p>
    <div class="answer">
      {{ form_start(form) }}
        {{ form_widget(form) }}
      {{ form_end(form) }}
    </div>
  </div>
{% endblock %}
